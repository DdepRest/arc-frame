# PROJECT_OVERVIEW.md

## Что это за проект

**A.R.C. Frame** — это десктопное приложение на C# для расчёта стоимости москитных сеток и сопутствующих товаров (отливы, козырьки, короба, уплотнения и др.).

Приложение предназначено для менеджеров по продажам и замерщиков, которые на объекте или в офисе быстро считают стоимость заказа и формируют коммерческое предложение (КП) для клиента.

## Для кого

- Для сотрудников, которые продают и устанавливают москитные сетки и оконную фурнитуру.
- Владелец проекта — не программист. Все технические решения должны быть простыми и предсказуемыми.

## Основной сценарий работы

1. Пользователь выбирает товар (Anwis, Отлив, Козырёк, ПСУЛ и т.д.).
2. Указывает размеры (ширину, высоту), цвет, количество.
3. Система автоматически рассчитывает площадь/периметр/количество и стоимость.
4. Добавляются позиции — общая сумма пересчитывается в реальном времени.
5. Заполняются данные заказчика (имя, телефон, адрес, номер договора).
6. Формируется КП (коммерческое предложение) — печать или сохранение в PDF.
7. При необходимости формируется текст для отправки на завод/производство.
8. Заказ сохраняется в историю, откуда его можно открыть, экспортировать или удалить.

## Технический стек

| Компонент | Технология |
|-----------|-----------|
| Язык | C# 12 |
| Фреймворк | .NET 8 |
| UI | WPF (Windows Presentation Foundation) |
| Тип приложения | WinExe, single-file publish |
| Целевая ОС | Windows 10/11 |
| Архитектура | win-x64 |
| Внешние зависимости | QuestPDF (для PDF-экспорта) |

## Ключевые функции

- **Расчёт стоимости** — автоматический пересчёт по формулам для каждого типа товара.
- **Режимы размера Anwis** — 5 режимов (ББ 60, ББ 70, ПП, Проём, Габарит) с коррекцией размеров.
- **Коммерческое предложение** — `FlowDocument` с SVG-чертежами (WPF `FlowDocumentPageViewer`), печать через `PrintQueueManager`, экспорт в PDF через `PdfExportService`.
- **Отправка на завод** — формирование структурированного текста с размерами для производства.
- **История заказов** — сохранение, загрузка, экспорт/импорт, сортировка, поиск.
- **Доп. КП** — возможность прикреплять дополнительные коммерческие предложения.
- **Автообновление** — проверка и установка новых версий через GitHub Releases.
- **Тёмная тема** — полноценная тёмная тема оформления с мгновенным переключением.
- **Undo/Redo** — отмена и повтор изменений (Ctrl+Z / Ctrl+Y).

## Что особенно важно не сломать

1. **Расчёты стоимости** — любая ошибка = неправильная цена для клиента.
2. **Формулы Anwis** — сложная логика коррекции размеров, легко допустить утечку формул на не-Anwis товары.
3. **Печать КП** — клиент видит именно этот документ.
4. **Отправка на завод** — производство работает по этим размерам.
5. **Автообновление** — если сломать, пользователи не получат исправления.
6. **Сохранение заказов** — данные клиентов и расчёты должны сохраняться надёжно.

## Source files

- `MosquitoNetCalculator/MosquitoNetCalculator.csproj` — версия, зависимости, настройки сборки.
- `MosquitoNetCalculator/App.xaml.cs` — точка входа, инициализация.
- `README.md` — описание проекта, инструкции по сборке и тестированию.

## Last verified

2026-07-15 — документ синхронизирован с текущим стеком: убрана устаревшая зависимость `Microsoft.Web.WebView2` (печатный конвейер переписан на нативный WPF `FlowDocumentPageViewer`), оставлена `QuestPDF` для PDF-экспорта. Описание проекта, стек, сценарии и критичные области актуальны.

2026-06-27 (A.R.C. v4 — SYMBOL_INDEX, INTENTS, arc-check)
