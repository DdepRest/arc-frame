<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- ═══════════════════════════════════════════════════════ -->
    <!-- CHECKBOX — Windows 11 Toggle Switch                     -->
    <!-- ═══════════════════════════════════════════════════════ -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="SwitchTrack" Width="40" Height="20"
                                CornerRadius="10" Margin="0,0,8,0"
                                Background="{DynamicResource TrackBg}" BorderThickness="0"
                                SnapsToDevicePixels="True">
                            <Border x:Name="SwitchThumb" Width="16" Height="16"
                                    CornerRadius="8" Background="{DynamicResource Surface}"
                                    HorizontalAlignment="Left" Margin="2,0,0,0"
                                    VerticalAlignment="Center">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.12" Direction="270"/>
                                </Border.Effect>
                            </Border>
                        </Border>
                        <ContentPresenter Grid.Column="1"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource Accent}"/>
                            <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,2,0"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource BorderHover}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource AccentHover}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════ -->
    <!-- SignToggleCheckBox — large +/- toggle for installation sum -->
    <!-- ═══════════════════════════════════════════════════════ -->
    <Style x:Key="SignToggleCheckBox" TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="SwitchTrack" Width="48" Height="26"
                            CornerRadius="13"
                            Background="Transparent"
                            BorderBrush="{DynamicResource Border}"
                            BorderThickness="1"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <!-- Background signs -->
                            <TextBlock Text="−" Foreground="{DynamicResource OnDanger}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,9,1" FontSize="14" FontWeight="Bold"/>
                            <TextBlock Text="+" Foreground="{DynamicResource OnSuccess}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="9,0,0,1" FontSize="14" FontWeight="Bold"/>

                            <!-- Thumb -->
                            <Border x:Name="SwitchThumb" Width="22" Height="22"
                                    CornerRadius="11" Background="{DynamicResource Surface}"
                                    HorizontalAlignment="Left" Margin="2,0,0,0"
                                    VerticalAlignment="Center"
                                    RenderTransformOrigin="0.5,0.5">
                                <TextBlock x:Name="ThumbText" Text="−"
                                           Foreground="{DynamicResource Danger}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           FontSize="15" FontWeight="Bold" Margin="0,0,0,1"/>
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.2" Direction="270"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="SwitchTrack" Property="BorderBrush" Value="{DynamicResource Success}"/>
                            <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,2,0"/>
                            <Setter TargetName="ThumbText" Property="Text" Value="+"/>
                            <Setter TargetName="ThumbText" Property="Foreground" Value="{DynamicResource Success}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SwitchTrack" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="SwitchThumb" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.92" ScaleY="0.92"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
