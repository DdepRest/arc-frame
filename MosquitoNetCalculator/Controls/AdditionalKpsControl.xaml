<UserControl x:Class="MosquitoNetCalculator.Controls.AdditionalKpsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="CardAdditionalKp" Background="{DynamicResource SectionBg}"
            CornerRadius="8" Padding="14" Margin="0,0,0,0">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Style="{DynamicResource FluentIconSection}" Text="&#xE8A5;"/>
                <TextBlock Text="ДОПОЛНИТЕЛЬНОЕ КП" Style="{DynamicResource SectionLabel}" Margin="0"/>
            </StackPanel>
            <CheckBox x:Name="ChkAdditionalKp"
                      Content="Есть доп. КП"
                      IsChecked="{Binding ClientInfo.HasAdditionalKp}"
                      Margin="0,0,0,6"
                      FontSize="11" Foreground="{DynamicResource TextPrimary}"/>

            <!-- Additional KP list -->
            <StackPanel Visibility="{Binding IsChecked, ElementName=ChkAdditionalKp, Converter={StaticResource BoolToVis}}">
                <Border Margin="-8,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="2"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="2"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="2"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Вкл." FontSize="9" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Width="48" TextAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="№ КП" FontSize="9" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Width="60" TextAlignment="Center"/>
                            <TextBlock Grid.Column="4" Text="Сумма, руб." FontSize="9" Foreground="{DynamicResource TextMuted}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding ClientInfo.AdditionalKps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,2" Height="28">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Opacity" Value="0.45"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="2"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="2"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="2"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center" HorizontalAlignment="Center"
                                                  ToolTip="Вкл / Выкл" Margin="6,0,2,0"/>
                                        <TextBox Grid.Column="2" Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}"
                                                 GotFocus="SelectAll_OnFocus"
                                                 ToolTip="{Binding Number}"
                                                 VerticalAlignment="Center" Height="26"
                                                 Width="Auto" MinWidth="40" MaxWidth="60">
                                            <TextBox.Template>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"
                                                            BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}"
                                                            BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource TemplatedParent}}"
                                                            CornerRadius="4" SnapsToDevicePixels="True">
                                                        <ScrollViewer x:Name="PART_ContentHost" Margin="2,0"
                                                                      VerticalAlignment="Center" Focusable="False"
                                                                      HorizontalScrollBarVisibility="Hidden"
                                                                      VerticalScrollBarVisibility="Hidden"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsFocused" Value="True">
                                                            <Setter TargetName="PART_ContentHost" Property="Margin" Value="2,0"/>
                                                            <Setter Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="BorderBrush" Value="{DynamicResource BorderHover}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </TextBox.Template>
                                        </TextBox>
                                        <TextBox Grid.Column="4"
                                                 HorizontalContentAlignment="Right"
                                                 GotFocus="SelectAll_OnFocus"
                                                 Loaded="AdditionalKpAmountTextBox_Loaded"
                                                 Unloaded="AdditionalKpAmountTextBox_Unloaded"
                                                 TextChanged="OnAmountTextChanged"
                                                 LostFocus="OnAmountLostFocus"
                                                 VerticalAlignment="Center" Height="26"
                                                 MinWidth="80">
                                            <TextBox.Template>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"
                                                            BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}"
                                                            BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource TemplatedParent}}"
                                                            CornerRadius="4" SnapsToDevicePixels="True">
                                                        <ScrollViewer x:Name="PART_ContentHost" Margin="2,0"
                                                                      VerticalAlignment="Center" Focusable="False"
                                                                      HorizontalScrollBarVisibility="Hidden"
                                                                      VerticalScrollBarVisibility="Hidden"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsFocused" Value="True">
                                                            <Setter Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="BorderBrush" Value="{DynamicResource BorderHover}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </TextBox.Template>
                                        </TextBox>
                                        <Button Grid.Column="6" Style="{DynamicResource DeleteRowButton}"
                                                Click="BtnDeleteAdditionalKp_Click"
                                                ToolTip="Удалить" Width="16" Height="16"
                                                VerticalAlignment="Center" HorizontalAlignment="Center" Padding="0">
                                            <Path Data="M3,3 L9,9 M9,3 L3,9"
                                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                  StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                                  Width="8" Height="8" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Button Content="+ Добавить КП"
                                Click="BtnAddAdditionalKp_Click"
                                FontSize="10" Foreground="{DynamicResource Accent}"
                                Background="Transparent" BorderThickness="0"
                                Cursor="Hand" Padding="2,2"
                                HorizontalAlignment="Left" Margin="0,4,0,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"
                                            CornerRadius="4" Padding="{Binding Padding, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                            <Setter Property="Background" Value="{DynamicResource AccentLight}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
