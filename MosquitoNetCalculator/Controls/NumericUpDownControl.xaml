<UserControl x:Class="MosquitoNetCalculator.Controls.NumericUpDownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="GhostRepeatButton" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Delay" Value="300"/>
            <Setter Property="Interval" Value="50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentLight}"/>
                                <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentLight}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- п.1 Fluent-обёртка: rounded Border c Surface-фоном и Border-обводкой,
         сейчас поле ввода визуально читается на любой теме (раньше цифра «терялась»). -->
    <Border Background="{DynamicResource Surface}"
            BorderBrush="{DynamicResource Border}"
            BorderThickness="1"
            CornerRadius="6"
            Height="32"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="32"/>
            </Grid.ColumnDefinitions>

            <RepeatButton x:Name="MinusBtn" Grid.Column="0"
                          Style="{StaticResource GhostRepeatButton}"
                          Content="−" Click="Minus_Click"
                          Width="32" Height="32"
                          VerticalAlignment="Center"/>
            <!-- ValueTextBox: явные Fluent-цвета + прозрачный фон/без рамки,
                 чтобы цифра была визуально центром всего контрола. -->
            <TextBox x:Name="ValueTextBox" Grid.Column="1"
                     TextAlignment="Center"
                     VerticalContentAlignment="Center"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="{DynamicResource TextPrimary}"
                     FontWeight="SemiBold"
                     FontSize="14"
                     CaretBrush="{DynamicResource Accent}"
                     SelectionBrush="{DynamicResource Accent}"
                     Margin="4,0"
                     PreviewTextInput="ValueTextBox_PreviewTextInput"
                     DataObject.Pasting="ValueTextBox_Pasting"
                     LostFocus="ValueTextBox_LostFocus"
                     TextChanged="ValueTextBox_TextChanged"/>
            <RepeatButton x:Name="PlusBtn" Grid.Column="2"
                          Style="{StaticResource GhostRepeatButton}"
                          Content="+" Click="Plus_Click"
                          Width="32" Height="32"
                          VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
