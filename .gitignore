## .NET / WPF / Velopack build outputs
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs
publish/
artifacts/
validation-obj/

## Inno Setup installer output
Output/

## Velopack (Velopack 1.2.0 NuGet is used for auto-update hooks;
## release distribution is via GitHub Releases -- not Velopack Flow)
*.nupkg
*.snupkg
.nuget/

## Test / coverage artifacts
TestResults/
coverage.cobertura.xml
coverage.xml
*.coverage
*.trx

## IDE / editor
.vs/
.vscode/
.idea/
*.ide
*.dbmdl
*.swp
*.tmp

## OS junk
.DS_Store
Thumbs.db
desktop.ini
nul
_nul

## Build logs
*.log
InnoSetup.log

## Local-machine settings (should NEVER enter the repo)
appsettings.Development.json
settings.local.json
prices.local.json
prices-bak.json

## Office-report gist token (админ-панель) — подставляется в сборку локально,
## в git не попадает. Альтернатива: env var OFFICE_REPORT_TOKEN.
.office-report-token

## archive/ — корзина мусора (разбор владельцем; агентам не заходить, CHEATSHEET #22)
archive/

## Freebuff agent workspace (per-thread git worktrees — never commit)
.freebuff/

## Local release dry-run artifacts (ZIP/publish output; final ZIPs go to GitHub Releases)
release-artifacts/
